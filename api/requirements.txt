# Minimal dependencies for Vercel serverless deployment
# Heavy scientific libraries (thermo, chemicals, cantera, coolprop) are excluded
# to stay under the 250 MB function size limit

fastapi>=0.100
pydantic>=2.0
pyyaml>=6.0
